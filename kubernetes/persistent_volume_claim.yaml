apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-storage-persisent-volume-claim
spec:
  volumeName: db-storage-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
